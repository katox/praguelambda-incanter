(ns praguelambda-incanter.core-test
  (:require [clojure.test :refer :all]
            [praguelambda-incanter.core :refer :all]))

(deftest a-test
  (testing "I have no tests and I must SCREAM."
    (is (= :true :true))))
